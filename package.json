{
  "name": "x-autopost",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Interview-driven X auto-posting on Google Apps Script + Spreadsheet, with Slack/Notion integration. (server/ and android/ are the legacy home-server version.)",
  "scripts": {
    "start": "node server/src/index.js",
    "tick": "node server/src/tick.js",
    "generate:week": "node server/src/generate-week.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
