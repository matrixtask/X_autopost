# セットアップガイド（Google Apps Script版）

所要時間の目安: 30〜45分。上から順に進めてください。

## 0. 必要なもの

| サービス | 用途 | 必須 |
| --- | --- | --- |
| Googleアカウント | GAS + スプレッドシート（DB） | ✅ |
| Anthropic APIキー | 質問生成・下書き生成・採点 | ✅ |
| Slackワークスペース | インタビュー対話・通知 | ✅ |
| X Developer（Free枠でOK） | 自動投稿 | 投稿時のみ |
| Notionインテグレーション | ストックのデータベース閲覧 | 任意 |

## 1. スプレッドシートとGASプロジェクト

1. 新しいGoogleスプレッドシートを作成し、URLの `/d/` と `/edit` の間のIDを控える
2. [script.google.com](https://script.google.com) で新規プロジェクトを作成
3. `gas/src/` の各ファイルをプロジェクトに追加する
   - `*.js` はスクリプトファイル（GAS上では `.gs` になる）、`Index.html` はHTMLファイルとして作成
   - claspを使う場合は下の「CLIでの反映（clasp）」を参照（以後の更新がコマンド1発になるので推奨）
4. プロジェクトの設定 > スクリプト プロパティに以下を登録:

| プロパティ | 値 |
| --- | --- |
| `SPREADSHEET_ID` | 手順1のID |
| `ANTHROPIC_API_KEY` | AnthropicのAPIキー |
| `ADMIN_TOKEN` | 長いランダム文字列（Webアプリの認証用） |
| `DRY_RUN` | `true`（最初は必ずtrueのまま） |

5. GASエディタで `setupSpreadsheet` を選んで実行（初回は権限承認ダイアログが出る）
   → シート（Stock / Interviews / Themes / Voice / Log）とテーマ初期データが作られる

## 2. 文体の登録（いちばん大事）

スプレッドシートの **Voice** シートのA列に、**自分が実際に書いたポストや文章を20〜50本**貼ってください。これが文体再現の材料（few-shot）と採点基準になります。多いほど「自分っぽく」なります。B列は任意のメモです。

**Themes** シートは自由に編集できます。`category` は `evergreen`（定番）/ `news`（時事）/ `neta`（ネタ）の3種類、`weight` が大きいほど選ばれやすくなります。

## 3. Webアプリのデプロイ（スマホ確認UI + Slack受け口）

**デプロイは2本に分けます。** Slackは匿名でPOSTしてくるためGoogle認証を掛けられませんが、
人間が使う管理UIはドメイン制限で守ります。

### A. 管理UI用（人間が使う。ドメイン制限）

1. デプロイ > 新しいデプロイ > 種類: **ウェブアプリ**
   - 実行ユーザー: **自分**
   - アクセスできるユーザー: **自社ドメイン内のユーザー** ← ここを「全員」にしない
2. スクリプトプロパティを設定
   - `ALLOWED_DOMAINS` = `tetra-aviation.com`（既定値。複数はカンマ区切り）
   - `ALLOWED_EMAILS` = 社外の共同編集者を個別に許可する場合のみ
3. 発行URLを `WEBAPP_URL` に保存。スマホでは会社のGoogleアカウントでログインした状態で
   `WEBAPP_URL?token=ADMIN_TOKEN` を開き、ホーム画面に追加

`REQUIRE_TOKEN` = `false` にすると、URLから `?token=` を省けます（Googleアカウント認証のみになる）。
ドメイン制限が効いていることを確認してから切り替えてください。

### B. Slack用（Event Subscriptions専用。全員アクセス）

1. デプロイ > 新しいデプロイ > 種類: ウェブアプリ / 実行ユーザー: 自分 / アクセス: **全員**
2. このURLはSlackのRequest URLにだけ設定し、人には配らない
3. このURLでUIを開こうとしても匿名アクセスなのでコード側が必ず拒否します

### どちらのデプロイがどちらか分からなくなったら

**GASの「デプロイを管理」を開いて、`アクセスできるユーザー` の列を見てください。** これが唯一の確実な見分け方です。

| 表示 | どちら | 用途 | URLの保管場所 |
| --- | --- | --- | --- |
| 自社ドメイン内のユーザー | **A（管理UI）** | 人間がスマホ/PCで開く | スクリプトプロパティ `WEBAPP_URL` |
| 全員 | **B（Slack）** | Slackの受け口だけ。人には配らない | SlackアプリのRequest URL |

URLからの逆引きもできます。

- `WEBAPP_URL` に入っているURL → **A**
- SlackアプリのEvent Subscriptionsに入っているURL → **B**

ブラウザで開いても判別できます。会社アカウントでログインした状態で開いて、**アプリが表示されれば A**、**アクセスを拒否されれば B** です。

### コードを更新したときの反映

`clasp push` だけでは**Webアプリには反映されません**（時間トリガーの実行分には即反映されます）。Webアプリは「デプロイを管理 > 編集 > 新バージョン」で更新します。URLは変わりません。

毎回どちらを更新するか考えなくて済むよう、`deploy.sh` にIDを1度登録しておけば、以後は両方まとめて更新されます。

```bash
./deploy.sh --save-admin AKfycb...   # 管理UI用（1度だけ）
./deploy.sh --save-slack AKfycb...   # Slack用（1度だけ）

./deploy.sh          # push + 登録済みを全部更新（以後はこれだけ）
./deploy.sh --list   # いま何が登録されているかを確認
```

> 管理UIのアクセス制御は `Session.getActiveUser().getEmail()` に依存します。万一メールが
> 取得できずロックアウトされた場合は `ACCESS_MODE` = `token` で旧方式（トークンのみ）へ
> 一時退避できます。原因を直したら `account` に戻してください。

## 4. Slackアプリ

1. [api.slack.com/apps](https://api.slack.com/apps) > Create New App > From scratch
2. **OAuth & Permissions** > Bot Token Scopes に `chat:write` / `channels:history`（プライベートチャンネルなら `groups:history`）/ `files:read` を追加し、ワークスペースにインストール
   - `files:read` は画像つきの返信を読むために必要。無いと画像は無視され、テキストだけが回答になる
3. Bot User OAuth Token（`xoxb-...`）をスクリプトプロパティ `SLACK_BOT_TOKEN` に保存
4. インタビュー用チャンネルを作成し、`/invite @ボット名` で招待。チャンネルIDをプロパティ `SLACK_CHANNEL_ID` に保存
5. **Event Subscriptions** を有効化:
   - Request URL に手順3のWebアプリURLを入力（Verifiedと出ればOK）
   - Subscribe to bot events に `message.channels`（プライベートなら `message.groups`）を追加して保存

> **セキュリティ注**: GASはリクエストヘッダを読めないためSlack署名検証ができません。代わりにチャンネルID一致チェックとevent_id重複排除で防御しています。インタビューチャンネルには本人だけを入れてください。

## 5. X API（投稿を有効にするとき）

1. [developer.x.com](https://developer.x.com) でアプリを作成（Free枠で月500件まで投稿可）
2. User authentication settings で **Read and Write** を有効化
3. Keys and tokens から4つの値をスクリプトプロパティに保存:
   `X_API_KEY` / `X_API_SECRET` / `X_ACCESS_TOKEN` / `X_ACCESS_SECRET`
   （Access Tokenは権限変更後に**再生成**したものを使うこと）
4. 動作確認が済むまで `DRY_RUN` は `true` のまま

## 6. Notion同期（任意）

1. [notion.so/my-integrations](https://www.notion.so/my-integrations) でインテグレーションを作成し、シークレットを `NOTION_TOKEN` に保存
2. Notionでデータベースを作成し、以下のプロパティを用意:
   - `Name`（タイトル）/ `Status`（セレクト）/ `Category`（セレクト）/ `Score`（数値）/ `Scheduled`（日付）/ `Posted`（日付）/ `Body`（テキスト）
3. データベースをインテグレーションに共有（… > コネクト）し、データベースIDを `NOTION_DATABASE_ID` に保存

## 7. トリガー登録（ルーティンの起動）

GASエディタで `installTriggers` を実行すると以下が登録されます:

| タイミング | 関数 | 内容 |
| --- | --- | --- |
| 毎朝8時台 | `startDailyInterview` | テーマ選定 → Slackにインタビュー |
| 毎晩21時台 | `nightlyGateAndSchedule` | 採点 → 予約割当 → Slack通知 |
| 毎時 | `postTick` | 予約時刻を過ぎたものを投稿 |
| 毎週月曜9時台 | `weeklyDigest` | ストック残量・先週の実績を通知 |
| 毎週月曜10時台 | `weeklyThemeMaintenance` | テーマのメモ書き直し + スタメン入れ替え |

## 8. 動作確認 → 本番切り替え

1. `startDailyInterview` を手動実行 → Slackに質問が来る → スレッドで返信 → 下書きがStockシートに入ることを確認
2. `nightlyGateAndSchedule` を手動実行 → 採点・予約とSlack通知を確認
3. `postTick` を手動実行 → `[DRY RUN]` 通知で内容を確認
4. 問題なければ:
   - `DRY_RUN` を `false` に（実投稿が始まる）
   - 品質ゲートを信用できるようになったら `AUTO_APPROVE` を `true` に（承認タップも不要になる）

## 9. 採点軸の学習（標本を一気に増やす）

全体スコアは「17軸の採点 × 各軸の成果相関」の内積です。相関は実測から学習するので、
標本が少ないうちは既定重みのままです。過去のポストを遡って標本にするには、
GASエディタで次の順に実行します（どちらも時間がかかるので放置してOK）:

1. `backfillManualPosts` — 自分の過去ポストをページネーションで遡って `Stock` に取り込む（既定1000件、Xの上限は3,200件）
2. `backfillAxisScores` — 軸スコアが無い投稿済みポストをまとめて採点する。6分の実行上限に当たる前に自分で続きのトリガーを仕込むので、完了通知がSlackに来るまで待つだけ
3. `reportAxisAnalysis` — 相関分析をSlackに投げる（Webアプリの「分析」タブにも出ます）

`backfillAxisScores` の進捗は `Log` シートに `backfill_axes` として1バッチ（30〜60秒）ごとに出ます。
継続トリガーの発火中に手動実行すると二重採点になるため、後から始めた方はスキップされます
（「既に実行中です」とログに出ます）。

遡及採点が `backfill_error` で止まったときは **`debugBackfillSample`** を実行してください。
未採点の先頭3件だけを採点してClaudeの生の応答をログに出します（シートは書き換えません）。
空応答なのか・形式違いなのか・途中で切れたのかがそこで分かります。

学習は2本立てです。フォロワー増 = インプレッション × プロフィールクリック率 × フォロー率 なので、両方を足して重みにしています。

| モデル | 成果指標 | 標本 | 役割 |
| --- | --- | --- | --- |
| リーチモデル | 前後30日内でのインプレッション順位 | 最大3,200件 | 何が読まれるか |
| フォローモデル | プロフィールクリック率 | 直近30日分のみ（X APIの制約） | 何がフォローに繋がるか |

インプレッションは時期によって桁が変わるため、生の値ではなく前後30日の投稿内での相対順位（パーセンタイル）に変換してから相関を取っています。

### 採点が当てになるかを確かめる

`diagnoseScoring`（`Metrics.gs`）を実行すると、次の3つがまとめて出ます。

1. **採点は再現するか** — 同じポストを2回採点して軸ごとに一致するか。一致しない軸は、重みをどう調整しても意味がないので自動的に重み0にします（`measureScoringReliability` 単体でも実行できます / `Quality.gs`）
2. **採点は当たるか** — 5分割クロスバリデーション。学習に使っていない投稿を採点して実測を当てられるか（`evaluateScoringAccuracy` 単体でも実行できます / `Metrics.gs`）
3. **重みの出どころ** — 事前の想定と実測の比率、実測で効いていると言い切れる軸

**手元のデータへの当てはまりと、当たるかどうかは別物です。** 実測（n=66）で確かめたところ、
同じ標本に当てはめた相関は +0.32 でしたが、学習に使っていない投稿では +0.04 でした。
重みを増やせば前者はいくらでも上がるので、判断には必ず 2 の数字を使ってください。

相関の扱いには次のガードを入れています。

- **偶然の範囲の相関は重み0にする。** 標本数から決まる標準誤差（フィッシャーz上で `1/√(n-3)`）で
  `AXIS_SIG_Z` 本ぶん割り引き、0を跨いだら捨てます。n=66 なら |r| が 0.31 を超えないと採用されません
- **有意な軸が揃うまでは事前分布を使う。** 実測で上書きすると、有意な軸が0本のとき全重みが0になり、
  すべてのポストが同点になります
- **中身が同じ軸は重みを分け合う。** 実測では 専門性・新規情報性・内部情報性・プロフィール誘引 が
  r=0.74〜0.90 でほぼ同一でした。内積で足すとこの1成分が4回数えられます
- **採点基準の自動学習（`evaluateScoring`）も、相関が誤差の範囲のときは更新しない。**
  偶然の共通点を基準にすると、それが次の採点に効いてノイズが自分を強化します

### 成果指標の選び方

`evaluateScoringAccuracy` は、プロフィールクリック付きの投稿が25件以上あればそちらを、
無ければインプレッション順位を使います。**インプレッションは内容でほとんど動きません** —
実測では17軸のどれも、文字数も投稿時刻も、有意な相関を持ちませんでした（80%の投稿が124〜463の幅に収まる）。
一方プロフィールクリック率に対しては、学習に使っていない投稿でも順位相関 +0.26 が出ています。
採点の良し悪しを判断するときは、必ずどちらの指標で測ったかを見てください。

### 下限を設けている軸

**本人らしさ**は成果指標ではなく制約条件です。実測で負の相関が出ても、重みを `AXIS_FLOORS`（既定 `{"voice": 0.05}`）より下げません。本人の文体で書くことはこの仕組みの前提であって、フォロワーが増えないなら捨てる、という性質のものではないためです。

下限をかけても分析レポートには生の相関がそのまま出るので、「データはこう出ているが重みは下限で止めている」ことは隠れません（該当軸に「下限で固定」と表示されます）。

### カテゴリごとに軸を分ける

相関は全カテゴリを混ぜて出しているので、ネタと真面目が打ち消し合います。その平均でネタを採点すると**ユーモアを削るほど点が上がり**、ネタでも真面目でもない中途半端な文になります。

そこで、カテゴリの定義そのものにあたる軸は成果指標ではなく制約として扱い、下限を設けています（`CATEGORY_FLOORS`）。

| カテゴリ | 下限を設ける軸 | 理由 |
| --- | --- | --- |
| `neta` | ユーモア 0.18 / 簡潔さ 0.08 | 笑えないネタはネタではない |
| `news` | 新規情報性 0.10 / 専門性 0.05 | 既知の受け売りなら出す意味がない |
| `evergreen` | 内部情報性 0.08 | 当事者にしか書けない部分を残す |

実測値で検算すると、笑えるネタ54点 / 笑いを削ったネタ42点になります（下限が無いとどちらも46点で差が付きません）。

生成側にも「**1つのポストの中でネタと真面目を混ぜない**」を明示し、採点側にも「どっちつかずは低く付ける」を入れてあります。

### 学習が効く3か所

採点だけを学習しても、聞くテーマと質問が変わらなければ同じような下書きしか出てきません。相関は次の3か所に効きます。

| 場所 | 何が変わるか |
| --- | --- |
| テーマ選定 | `weight` を実績で更新。`base_weight` を起点に増減するので、掛け続けて発散しない |
| 質問生成 | 効いている軸を引き出す質問を優先。スキップされた質問・ほとんど答えてもらえなかった質問を「避ける型」として渡す |
| 採点 | 軸スコアと相関ベクトルの内積 |

重みの決め方は次のとおりです。**標本が増えるほど `base_weight`（手で決めた初期値）から離れ、実測が支配します。**

```
s      = n / (n + K)                    証拠の強さ 0〜1
anchor = base_weight * (1 - s) + 中立値 * s
weight = anchor * (1 + s * (perf - 50) / span)
```

手の勘をいつまでも起点に残すと、証拠47件のテーマが証拠2件のテーマより軽くなる、という逆転が起きます（実際に起きました）。証拠が薄いうちは `base_weight` を尊重し、溜まったら捨てる形にしてあります。

表記ゆれで重複したテーマは **`reportDuplicateThemes`** で統合できます。行は消さず、重複側を重み0の停止テーマにしてポストを代表名へ寄せるので、間違っていれば手で戻せます。

テーマ重みは `weeklyMetricsReport` から自動更新されます。手動なら **`reportThemeWeights`** です。`Themes` シートの `perf`（窓内順位の平均）と `perf_n`（標本数）を見れば、なぜその重みになったか分かります。実測が無いテーマは据え置かれます。

### スタメン100件と週次の入れ替え

テーマは**スタメン100件**を持ち、毎朝の選定はこの100件からのみ行います（`roster` 列）。下位50件は週1回まるごと入れ替えます。

| 枠 | 件数 | 中身 |
| --- | --- | --- |
| `core` | 50 | 実績上位。活用 |
| `adjacent` | 10 | 上位に似ているが同一ではないテーマ。近傍の探索 |
| `random` | 20 | 実績と無関係に選ぶ。遠方の探索（局所解に嵌らないため） |
| `trend` | 20 | 時事・トレンド。鮮度が命なので毎週必ず入れ替える |

実測が無いテーマは順位付けの中央（50点）に置きます。試されないまま消えることも、根拠なく上位に居座ることもないようにするためです。`random` 枠はまずベンチに眠っているテーマから引き、足りなければ新しく作ります。

**毎朝のインタビューには時事枠が必ず1つ入ります。** トレンドは翌週には語る意味が無くなるため、重み抽選に任せて出ない日が続くのを避けています。

入れ替えは `weeklyThemeMaintenance`（毎週月曜10時台）が実行します。手動なら **`reportThemeRoster`** です。`weeklyMetricsReport` とは別トリガーにしてあります。どちらもLLMを何度も呼ぶので、まとめると6分の実行上限に当たるためです。

### メモの書き直し（`reportThemeNotes`）

`notes` は質問生成のプロンプトにそのまま渡っています。手で書いた想定が外れていると、外れた方向へ質問が誘導され続けます（実測では「失敗談は強い」と書いた3テーマがすべて平均以下でした）。

実測が `NOTES_MIN_SAMPLES`（既定5）件以上溜まったテーマについて、実際に伸びた投稿と沈んだ投稿を見せたうえでメモを書き直します。「（出典: ◯◯）」の括弧書きは残すので、どこから来たテーマかは失われません。

### 手動投稿からの逆算（`inferManualPostSources`）

取り込んだ過去の投稿は `theme` が「手動投稿」で一括りなので、そのままではテーマ重みの学習に使えません。実測の大半がそこに死蔵されている状態です。

**`inferManualPostSources`** を実行すると、各投稿について「どのテーマの話か」「どんな質問をされたら出てきた話か」を推定し、`theme` と `inferred_question` に書き戻します。放置で完走します（4分で打ち切り、1分後に自動継続）。

これで2つが動き出します。

- **テーマ重み**: 数百件ぶんの実測がテーマ別に効くようになる
- **質問生成**: 実際に伸びた投稿を生んだ聞き方（窓内順位75点以上）を手本として渡せるようになる

どのテーマにも当てはまらない投稿が `NEW_THEME_MIN_POSTS`（既定3）件以上まとまると、新しいテーマとして `Themes` に自動追加されます。思いつきレベルの1〜2件は採用しません。

### 交絡の検証

**`reportAxesByOrigin`** を実行すると、取り込んだ手動投稿とこの仕組みが生成した投稿とで、軸の効き方が違うかどうかを比べます。群を分けて符号が反転する軸は、その軸そのものではなく「どちらの群か」を測っている疑いがあります。標本の大半は過去の手動投稿なので、この検証をしないと交絡に気づけません。

## 画像つきの返信

インタビューのスレッドに画像を添付して返信すると、Claudeが画像を読んで**事実として回答テキストに追記**します（`[画像から] …` の形）。以降の下書き生成・採点は文章として扱うので、特別な処理は要りません。

読み取った内容はSlackに返すので、違っていればそのまま書き直して送ってください。

**画像はXへの投稿にも添付されます。** その回答から作られた下書きが画像を引き継ぎ、投稿時にXへアップロードされます（v1.1 の `media/upload`）。Webアプリの一覧では画像つきの下書きに 🖼 が付きます。

画像そのものは保存せず、SlackのURLだけを持ち回って投稿直前に取り直します。どこかに複製を溜めるより壊れにくいためです。ただし**Slackでファイルを削除すると添付できなくなります**。その場合は本文だけを投稿し、Slackに警告を出します（予約枠を落とすより被害が小さいため）。

うまくいかないときは `Log` シートの `slack_file_error` を見てください。「ログインページが返っています」と出ていれば `files:read` の追加漏れです。

- 対応形式は PNG / JPEG / GIF / WebP。**形式はSlackの申告ではなく中身のバイト列で判定します**（認証に失敗するとログインページのHTMLが `image/png` として返るため）
- 元ファイルが未対応形式（HEIC等）や大きすぎる場合は、Slackのサムネイル（JPEG/PNGに変換済み）へ自動でフォールバックします
- 1回の返信につき `MAX_IMAGES_PER_REPLY`（既定3枚）まで
- `MAX_IMAGE_BYTES`（既定4MB）を超える画像は飛ばします
- 画像だけで本文が無い場合も、読み取れた内容を回答にします。読み取れなければ質問は消費せず、ひとこと促します
- 推測や感想は書かせず、看板の文字・型番・数字など**読み取れる具体**を拾わせています（実測で効いている軸が具体性と内部情報性のため）

## 生成に失敗したインタビューの作り直し

下書き生成でエラーが出た日は、回答だけが `Interviews` に残って `Stock` が空になります。
GASエディタで **`regenerateFailedInterviews`** を実行すると、
「回答はあるのに下書きが無い」セッションを探して生成→採点をやり直します。
回答は消えないので何度でも実行できます。1回で3セッションまで処理し、残りは次の実行に回ります。

## 調整できるプロパティ

| プロパティ | 既定値 | 意味 |
| --- | --- | --- |
| `QUALITY_THRESHOLD` | `75` | 品質ゲートの合格点（上げるほど厳選） |
| `SLOT_TIMES` | `08:00,12:30,19:30` | 1日の予約枠 |
| `MAX_POSTS_PER_DAY` | `3` | 1日の最大投稿数 |
| `INTERVIEW_QUESTIONS` | `4` | 毎朝の質問数 |
| `CLAUDE_MODEL` | `claude-sonnet-5` | 生成・採点に使うモデル |
| `BACKFILL_MAX_POSTS` | `1000` | `backfillManualPosts` が遡る件数 |
| `BACKFILL_BATCH` | `15` | `backfillAxisScores` が1回のAPI呼び出しで採点する件数（失敗すると自動で半減する） |
| `AXIS_SIG_Z` | `2.5` | 相関を何標準誤差ぶん割り引くか。これを超えない相関は重み0（＝偶然の範囲として捨てる）。下げるほど早く学習するが、ノイズを掴みやすい |
| `AXIS_LEARNED_FULL_AT` | `6` | 有意な軸がこの数に達したら重みを実測側へ全面的に寄せる。それまでは事前分布と按分する |
| `CLUSTER_SPLIT` | `learned` | 中身が同じ軸の塊で重みを分け合わせる範囲。`learned`=学習分だけ / `all`=事前分布も含む / `off`=分割しない |
| `CLUSTER_THRESHOLD` | `0.7` | 軸どうしの相関がこれ以上なら「同じものを測っている」とみなす |
| `RELIABILITY_SAMPLE` | `12` | `measureScoringReliability` が2回採点する件数 |
| `RELIABILITY_MIN_CORR` | `0.5` | 2回の採点がこの相関を下回る軸は使わない |
| `RELIABILITY_MAX_MAD` | `15` | 2回の採点の平均差がこれを超える軸は使わない |
| `MIN_AXIS_SAMPLES` | `10` | 相関を保存する最低標本数 |
| `REACH_MODEL_WEIGHT` | `0.5` | リーチモデルの相関を重みに足すときの割引率 |
| `AXIS_FLOORS` | `{"voice": 0.05}` | 相関が負でも重みをこの値より下げない軸 |
| `CATEGORY_FLOORS` | 下記参照 | カテゴリごとに外せない軸の下限 |
| `THEME_WEIGHT_SPAN` | `20` | テーマ実績が平均から何点ずれたら重みを倍にするか（小さいほど過激） |
| `THEME_SHRINKAGE_K` | `10` | テーマ重みを標本数で薄める強さ（大きいほど慎重） |
| `THEME_NEUTRAL_WEIGHT` | `2` | 標本が溜まったときに寄せる中立の重み |
| `INFER_BATCH` | `20` | `inferManualPostSources` が1回のAPI呼び出しで判定する件数 |
| `NEW_THEME_MIN_POSTS` | `3` | 逆算で見つかった新テーマを採用する最低件数 |
| `ROSTER_CORE` | `50` | スタメンのうち実績で残す件数 |
| `ROSTER_ADJACENT` | `10` | 上位に隣接する新テーマの件数 |
| `ROSTER_RANDOM` | `20` | 無作為枠の件数 |
| `ROSTER_TREND` | `20` | 時事枠の件数（毎週入れ替え） |
| `NOTES_MIN_SAMPLES` | `5` | メモを書き直す最低標本数 |
| `MAX_IMAGES_PER_REPLY` | `3` | 1回の返信で読む画像の枚数 |
| `MAX_IMAGE_BYTES` | `4000000` | これを超える画像は飛ばす |

## CLIでの反映（clasp / Ubuntu）

初回セットアップ:

```bash
# Node.js 20+ が無ければ
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash - && sudo apt-get install -y nodejs

sudo npm install -g @google/clasp
clasp login                     # ブラウザが開くのでGoogleアカウントで許可

# Apps Script APIを有効化（1回だけ。下のURLを開いてONにする）
#   https://script.google.com/home/usersettings

git clone https://github.com/matrixtask/X_autopost.git && cd X_autopost/gas
cp .clasp.json.example .clasp.json
#   .clasp.json の scriptId を記入（GASエディタ > プロジェクトの設定 > スクリプトID）

# WebアプリのデプロイIDを保存しておくと ./deploy.sh だけで済む
#   デプロイIDは WebアプリURLの /macros/s/◯◯◯/exec の ◯◯◯ 部分（AKfycb…）
echo 'AKfycb…' > .deployment-id
```

以後の反映はこれだけ:

```bash
cd X_autopost && git pull && cd gas && ./deploy.sh
```

- `clasp push` はローカルの `src/` をそのままGASに同期します（GAS側だけにあるファイルは消えるので、**手貼りしていた `コード.gs` 等が残っていれば初回pushで整理されます**。初回push後にGASエディタを開いて、`Code`という結合ファイルが二重に残っていないか確認してください）
- push だけでも時間トリガー（インタビュー・採点・投稿）には即反映されます。**doGet/doPost（Webアプリ・Slack受信）への反映にはデプロイ更新が必要**で、`deploy.sh` がデプロイIDを使って同じURLのまま新バージョンにします

## トラブルシューティング

- **Slackに質問が来ない**: Logシートと GASの「実行数」画面でエラーを確認。`SLACK_BOT_TOKEN` / `SLACK_CHANNEL_ID` とボットのチャンネル招待を確認
- **スレッドに返信しても反応がない**: Event SubscriptionsのRequest URLがVerifiedか、`message.channels` を購読しているか、Webアプリのデプロイが最新かを確認
- **投稿が401/403で失敗**: Xのアクセストークンを Read and Write 権限にしてから再生成したか確認
- **文体がAIっぽい**: Voiceシートのサンプルを増やす。`QUALITY_THRESHOLD` を上げるのも有効
