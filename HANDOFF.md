# 引き継ぎ資料 — X Autopost

このリポジトリの開発を、別のAIアシスタントや人が引き継ぐための資料。
2026-09-10 時点。**コードと矛盾したらコードが正。**

この資料は往復を前提にしている。引き継いだ側は、作業のたびに末尾の「変更履歴」に追記すること。次に引き継ぐ相手（元の担当に戻ることもある）はそこから読み始める。

---

## 0. 最初の30分でやること

1. `CLAUDE.md` を読む。ファイル名に Claude とあるが、**中身はこのリポジトリの運用ルールで、担当がどのAIでも適用される**
2. `git clone` して `npm test`（Node 20 以上）。27件通れば環境は正常
3. この資料の §3（用語）と §5（設計判断）を読む。ここを飛ばしてコードを触ると、過去に落ちた穴にもう一度落ちる
4. ユーザーに次を頼む。自分の手元では取れないものがある:
   - GitHub リポジトリ `matrixtask/X_autopost` へのアクセス（PR を作ってマージするため）
   - スプレッドシート "X Autopost" の中身（`Stock` シートを CSV でエクスポートして渡してもらうのが確実。理由は §7）
   - 何か起きたときは `Log` シートの該当行を貼ってもらう

**自分では反映（デプロイ）できない。** GAS への反映はユーザーの Ubuntu 機で `clasp` を使って行う。自分の成果物は「マージ済みの PR」と「反映手順の案内」まで。

反映手順の定型文（毎回これを添える）:

```
cd ~/X_autopost && git pull origin main && cd gas && ./deploy.sh
```

列を増やしたときは `setupSpreadsheet`（`Sheets.gs`）の実行も案内する。

## 1. 何を作っているか

teTra aviation（eVTOL スタートアップ）CEO の中井佑さんの X（Twitter）アカウントを、本人の文体で自動運用する仕組み。**目的はフォロワーを増やすこと。** いいね数ではない。

アカウントは小規模（インプレッションは1投稿あたり数百）。ここは重要で、§5.2 の判断はこの規模だから成り立つ。

最初から決まっている要件:

- テーマは仕組み側が決め、毎朝 Slack で本人に会話形式のインタビューをする
- 回答からポストの下書きを作り、採点して、良いものだけ予約投稿する
- **文体は AI 的でなく本人の文体。** これは成果指標ではなく制約条件（§5.4）
- 時事ネタとネタポスト（笑わせる投稿）を混ぜる。**ネタと真面目は1ポスト内で混ぜない**
- ストック型。たくさん作って、良いものだけ出す
- クラウドで完結: GAS + スプレッドシート + Notion + Slack + スマホ用 Web UI

`server/` と `android/` は旧・自宅サーバー版で、いまは使っていない。本体は `gas/`。

**いちばん大事な入力は `Voice` シートの文体サンプル**（本人の過去ポスト）。生成プロンプト `buildStylePrompt`（`Voice.js`）と採点プロンプトの両方がこれを参照する。

## 2. 構成と1日の流れ

```
Slack（インタビュー・通知） ⇄ GAS（gas/src/*.js） ⇄ スプレッドシート（データ）
                                   ↓         ↓
                              Claude API    X API（投稿・計測）
                                   ↓
                              Notion（ストック同期・テーマ管理）
```

`gas/src/Foo.js` は clasp で GAS エディタの `Foo.gs` になる。

| ファイル | 役割 |
| --- | --- |
| `Config.js` | 定数、スクリプトプロパティ、採点軸 `AXES`（17軸）、重みの合成 `axisWeightBreakdown` |
| `Pure.js` | GAS 非依存の純粋関数。`test/gas-pure.test.js` が Node の `vm` で読み込んで検証する |
| `Sheets.js` | シート操作。`SHEET_HEADERS` が列定義の正 |
| `Claude.js` | LLM クライアント。用途別モデル、拒否・打ち切りの扱い |
| `Interview.js` | 朝のインタビュー、質問生成、Slack 返信の処理 |
| `Drafts.js` | 回答から下書き生成 |
| `Quality.js` | 品質ゲート、採点の共通処理 `scoreAxesForRows`、遡及採点、再現性の測定 |
| `Themes.js` | テーマ選定、スタメン100件の週次入れ替え、テーマ重みの学習 |
| `Metrics.js` | メトリクス収集、フォロワー記録、相関分析、予測力の検証、健康診断 `diagnoseScoring` |
| `Scheduler.js` | 承認済みを予約枠へ |
| `XApi.js` | X API（OAuth 1.0a）。投稿、v1.1 media/upload、枠切れの検知 |
| `WebApp.js` / `Index.html` | 管理UI（スマホ）と Slack の受け口 |
| `Slack.js` / `Notion.js` / `News.js` / `Voice.js` / `Memory.js` / `Rewrite.js` / `Triggers.js` | 各機能 |

トリガー（JST。`installTriggers` / `Triggers.gs` で登録）:

| 時刻 | 関数 | 内容 |
| --- | --- | --- |
| 毎日 8時台 | `startDailyInterview` | テーマを選び、質問を生成して Slack に投げる |
| 毎時 | `postTick` | 予約時刻を過ぎたポストを X へ |
| 毎日 21時台 | `nightlyGateAndSchedule` | 採点 → 不合格をリライトして再採点 → 予約 → Slack 通知 |
| 毎日 23時台 | `dailyFollowerSnapshot` | フォロワー数を記録 |
| 月曜 8時台 | `weeklyMetricsReport` | メトリクス取得 → 相関分析 → 予測力の検証 → テーマ重み更新 |
| 月曜 9時台 | `weeklyDigest` | 週次サマリ |
| 月曜 10時台 | `weeklyThemeMaintenance` | メモ書き直し、スタメン入れ替え |

ポストのステータス遷移:

```
draft（生成直後）→ 採点 → ready（合格・承認待ち）→ approved → scheduled → posted
                       └→ stock（閾値未満・保留）→ リライト → draft に戻る
                                                 └→ 管理UI「ストック全承認→予約」で approved（採点を飛ばす非常口）
```

## 3. 用語

この資料とコードのコメントで使う言葉。

| 用語 | 意味 |
| --- | --- |
| ストック / stock | 採点で閾値未満だったが捨てずに保持している下書き。リライトの種になる |
| 遡及採点 | 過去に投稿済みのポストに後から軸スコアを付けること。相関分析の標本を増やすため。`backfillAxisScores` |
| 手動投稿 / manual | 本人が手で投稿した過去ツイートを取り込んだ行。学習の標本。`importManualPosts` |
| スタメン / roster | 選定対象になるテーマ100件。core 50 / adjacent 10 / random 20 / trend 20 |
| 軸 / axes | 17項目の採点観点（本人らしさ、ユーモア、具体性、プロフィール誘引…）。`Config.js` の `AXES` |
| 窓内順位 | 前後30日の投稿の中でのインプレッションの相対順位。時期でフォロワー数が変わるので生の値を使わない |
| ハロー（halo） | 「良いポストは全軸が高い」という共通成分。軸ごとの効き方を見るときはこれを引く |
| 塊 / cluster | 互いに強く相関して実質同じものを測っている軸の集合。内積で何度も数えられるのを防ぐ |
| 物差し / anchor | 採点の50点の位置を固定するために毎回プロンプトへ載せる典型例3件。`SCORING_ANCHOR_IDS` |
| 一次情報 | 本人がインタビューで実際に話した内容。リライト時に捏造を防ぐためこれ以外の事実は足さない |
| 有意 | その相関が偶然の範囲を超えているか。標本数で決まる |

## 4. 運用ルール（`CLAUDE.md` の要点）

- **自分で作った PR は確認なしで squash マージしてよい**（2026-08-04 にユーザーが許可）。マージ後は `main` を同期し、反映手順を案内する
- ただし次は先に相談: 設計の分岐で結果が大きく変わるとき / 既存データを壊しうる変更（列・行の削除、シートの作り直し） / 推測でしか直せておらず、外れると症状が悪化するとき
- **関数を追加したら、GAS エディタのどのファイル（`◯◯.gs`）に入るかを必ず書く。** GAS の関数プルダウンは開いているファイルの関数しか出ない。リポジトリ上のパスも併記
- デプロイは2本ある。A = 管理UI（自社ドメイン限定、URL は `WEBAPP_URL`）、B = Slack 受け口（全員アクセス、URL は Slack の Request URL）。**`clasp push` だけでは Web アプリに反映されない。** `Index.html` や `doGet` を触ったら A、`doPost` を触ったら B の更新まで案内する。`gas/deploy.sh` は ID を登録済みなら両方更新する
- 戻すときは `git revert` して同じ反映手順。スクリプトプロパティは残るので、プロパティで切り替えた挙動は手で戻す
- セキュリティ: 管理UIは自社ドメインの Google アカウントだけがアクセスできる。これは崩さない

## 5. 設計判断と、なぜそうなっているか

**コードを変える前にここを読む。** 判断のほとんどは実データで検証した結果で、見た目の「改善」が実際には悪化だった例が複数ある。

### 5.1 採点の仕組みと、学習がノイズだった話

各ポストを17軸で LLM が 0〜100 に採点し、全体スコア = 軸スコアと重みベクトルの内積を 0〜100 に正規化したもの。閾値（`QUALITY_THRESHOLD`、既定75）以上で合格。

重みは実測から学習する設計だが、**2026-08-27 に実データ（66件）で検証したところ、学習が完全にノイズを拾っていた。**

| 重みの作り方 | 同じ標本への当てはまり | 学習に使っていない投稿 |
| --- | --- | --- |
| 当時のやり方（生の相関 × n/(n+K)） | +0.32 | **+0.04** |

手元では当たっているように見えて、実際には何も予測していなかった。原因と対処（PR #47）:

1. **偶然の範囲の相関を重みにしていた。** n=66 で偶然と言えなくなるのは |r| ≥ 0.31 からだが、17軸のどれも到達していないのに全部が重みになっていた。→ `shrinkCorrelation`（`Pure.js`）でフィッシャーz上の信頼下限まで割り引き、0を跨いだら重み0。閾値は `AXIS_SIG_Z`
2. **有意な軸が0本だと全重みが0になる。** → 事前分布（`DEFAULT_AXIS_WEIGHTS`）を土台に、有意な軸が増えるほど実測へ寄せる（`AXIS_LEARNED_FULL_AT`）
3. **同じものを測っている軸が何度も数えられていた。** 専門性・新規情報性・内部情報性・プロフィール誘引が r=0.74〜0.90 でほぼ同一。→ 完全連結クラスタリング（`correlationClusters`）で塊を検出し、塊の中で重みを分け合う。単連結だと鎖状に伸びて7軸が1塊になったので使わない
4. **学習用と運用時で採点の物差しが違った。** 遡及採点だけカテゴリ無し・短縮プロンプトだった。→ `scoreAxesForRows` に一本化。物差し（`SCORING_ANCHOR_IDS`）で50点の位置を固定。**物差しに実測は載せない**（載せると採点が「伸びた投稿への類似度」に化けて循環参照になる）

### 5.2 成果指標はプロフィールクリック率

**この規模のアカウントでは、インプレッションは内容でほとんど動かない。** 17軸のどれも、文字数も投稿時刻も有意な相関を持たず、80%の投稿が 124〜463 の幅に収まる。

一方 **プロフィールクリック率**（フォローの直前行動）に対しては、学習に使っていない投稿でも順位相関 +0.26 が出た。スコア上位半分は 0.69%、下位半分は 0.38%。

→ 採点そのものは壊れておらず、**測る対象が間違っていた**。`evaluateScoringAccuracy` はプロフィールクリック付きの投稿が25件以上あればそちらで測る。X の仕様でプロフィールクリックは直近30日分しか取れない。

上の数字はすべて 2026-08 時点の66件に対するもの。**いまの本番データでの値は `diagnoseScoring` を実行しないと分からない**（§8）。

### 5.3 「正しさ」は必ずホールドアウトで測る

`diagnoseScoring`（`Metrics.gs`）が一次指標。3つを一度に出す:

0. 見ている数字はいつのものか（最終計測日、計測タイミングのばらつき、枠切れ）
1. **再現するか** — 同じポストを2回採点して一致するか（`measureScoringReliability` / `Quality.gs`）。一致しない軸は `AXIS_UNRELIABLE` に入り重み0
2. **当たるか** — 5分割クロスバリデーション（`evaluateScoringAccuracy` / `Metrics.gs`）
3. 重みの出どころ — 事前分布と実測の比率

軸別分析（`reportAxisAnalysis`）に出る相関は「手元への当てはまり」で、重みをいじれば上がる。**判断には使わない。**

採点基準の自動学習（`evaluateScoring` の rubric）も、相関が誤差の範囲のときは更新しない。偶然の共通点を基準にすると、それが次の採点に効いてノイズが自己強化する。

### 5.4 制約条件としての軸

「本人らしさ」は成果指標ではなく制約。相関がいくら負でも重みを `AXIS_FLOORS`（既定 `{voice: 0.05}`）より下げない。カテゴリごとの下限もある（`CATEGORY_FLOORS`: ネタはユーモア、時事は新規性、evergreen は内部情報性）。

### 5.5 X API の読み取り枠

2026-08-26 に月次上限（`spend-cap-reached`）に当たった。原因は `fetchTweetMetrics` が投稿済みの行を**毎回すべて**読み直していたこと（取り込んだ過去の手動投稿を含む）。

- 数字が確定した投稿は読み直さない。判定は「投稿の古さ」ではなく**「記録されている数字が投稿から何時間後のものか」**（`metrics_age_h` 列）。枠切れで計測が飛ぶと投稿3日目の数字を持ったまま35日目になるので、投稿の古さで見ると未熟な数字を確定済みと誤認する
- 枠切れ（spend-cap / 429）は1回で打ち切る。**フォールバックで投げ直さない**（通らないリクエストで枠を削る）。復帰見込み（翌月1日）を `X_SPEND_CAP_UNTIL` に記録し、それまで投げない
- 手動投稿の取り込みは `since_id` で前回以降だけ
- 分析は計測経過 `METRICS_MIN_AGE_H`（48h）未満の数字を除外。計測タイミングと成果指標の相関を交絡としてチェックする

### 5.6 LLM は用途で分ける

生成（下書き・リライト）と採点でモデルを分けられる（PR #49）。

- 生成 = `CLAUDE_MODEL_GENERATE`。文体の質が直接ポストに出るので上位モデルにする価値がある。現在 `claude-fable-5-1` を設定する運用
- 採点 = `CLAUDE_MODEL_SCORE`。数百件を回すうえ、**途中で変えると過去のスコアと比較できなくなる**。変えるなら意図して
- 生成の思考の深さは `CLAUDE_EFFORT_GENERATE`（既定 medium）。思考が常時オンのモデルは定型作業で深く考えすぎて出力枠を使い切る
- 安全分類器の拒否は HTTP 200 で `stop_reason: "refusal"`。空応答と見分けて再試行しない

**ファイル名が `Claude.js` で、プロパティ名に `CLAUDE_` が付いているのは、これまで Claude API だけを使ってきたから。** 別プロバイダ（OpenAI GPT-6 Astra 等）を足すときの指針:

- 採点は Claude のままにする（スコアの連続性）。足すなら生成側
- `askClaudeJsonSalvageable(system, user, maxTokens, {purpose})` と同じシグネチャの別モジュールを作り、`purpose` からプロバイダとモデルを選ぶ口を1か所にまとめる。既存の呼び出し側は変えない
- 名前を全部リネームする作業は価値が無いのでやらない
- GPT-6 Astra は `gpt-6-astra`、$10/$50 per 1M で Fable 5.1 と同額（2026-09 時点）。API キーと課金は別

### 5.7 テーマと時事

スタメン100件（core 50 / adjacent 10 / random 20 / trend 20）を週次で入れ替える。毎日のインタビューに時事枠を必ず1つ含める。時事は Google ニュース RSS（`when:3d`）から13フィード、AI 偏重を避けて領域ごとにラウンドロビン（`balanceByArea`）。前日話題にしたものは避ける（`TREND_COOLDOWN_DAYS`）。

テーマ重みは `anchor = base*(1-s) + neutral*s; w = anchor*(1 + s*(perf-50)/span)`。以前は縮小を調整分にしか掛けておらず、n=2 のテーマが n=47 のテーマより重くなる逆転が起きた。

### 5.8 Slack

- チャンネルへの直接投稿は**メモ**として取り込む。インタビューの回答ではない（回答は質問スレッドへの返信）
- `handleInterviewReply` は**スレッドの ts から1セッションだけ**を特定する。複数セッションにまたがって回答を消費したり、無関係なセッションに thread_ts を焼き付けたりした過去がある（PR #40, #45）
- 画像: `file_share` を受け付け、`url_private_download` を Bot Token で取る。リダイレクトを自動で追うと Authorization が落ちるので手で追う。Slack の `mimetype` は信用せずマジックバイトで判定（ログインページの HTML が 200 で返る）。`files:read` スコープ追加後は **Reinstall to Workspace** が必要（403 の原因はほぼこれ）
- 画像は X にも添付する（v1.1 `media/upload`。multipart は OAuth 署名に含めない）

## 6. 実装上の落とし穴

- **`updateStockById` は1件ごとにシート全体を読み直す。** 数十件を超える一括更新では `readTable` が返す `_row` と `setColumnByRows` で直接書く
- **`appendRow` は1行 100ms 前後。** 数百行なら `appendRowsObj`
- **GAS の実行時間上限は6分。** 長い処理は4分で打ち切り、`after()` のワンショットトリガーで自分を再実行。二重実行は `LockService` で弾く（`backfillAxisScores` が例）
- **LLM に軸スコアを返させるときは配列形式。** 17軸をキー付きで書かせると1件 250 トークン近くかかり `max_tokens` で切れる
- 思考ブロックが出力枠を使い切って本文が0文字になることがある（`claude_empty`）。`askClaudeJson` 系は出力枠を3倍にして1回だけ投げ直す（上限 16000）。残高切れ・認証エラー（`CLAUDE_FATAL`）と拒否（`CLAUDE_REFUSED`）は投げ直さない
- **列を増やしたら `SHEET_HEADERS` に追記する。** `ensureHeaders` が末尾に足す。ヘッダ行は書き換えない
- clasp は v2.4.2。apt の `clasp` は別物（ASP ソルバー）
- Snowflake の tweet_id は桁が増える。文字列比較すると古い ID を新しいと誤判定する → `compareTweetIds`

## 7. 検証の作法

**推測で直さない。実データで確かめてから変える。** これまでの修正のほとんどは、実データを読んで初めて原因が分かったもの。「こう直せば良くなるはず」で入れた変更が、検証したら逆効果だった例がある（採点の重み学習）。

- スプレッドシートの読み方は環境で違う。前任は Google Drive の連携で読んだが、**エクスポートが `Stock` の先頭 90 行程度で切れていた。** 全体を見ているつもりで一部しか見ていない状態になりやすいので、必ず「読めた範囲」を明示する。確実なのはユーザーに CSV でエクスポートしてもらうこと
- GAS のコードは Node で動かせる。`Pure.js` → `Config.js` → 必要なファイル、の順に `vm.runInContext` で読み、`PropertiesService` / `readTable` / `UrlFetchApp` / `logEvent` / `notifySlack` を差し替える。LLM や X API を叩かずに挙動を確認できる。注意: `XApi.js` を読み込むと本物の `xApiGet` が定義されるので、差し替えは読み込みの**後**に行う
- `npm test` は `Pure.js` のテスト。統計関数や ID 比較のような「間違えると静かに壊れる」ものはここに置く
- 実行ログは `Log` シート。`claude_empty` / `claude_refusal` / `backfill_error` / `x_spend_cap` / `scoring_accuracy` / `interview_no_match` などのイベント名で追える。原因が分からないときは「次に起きたとき原因が分かるログ」を先に入れる

## 8. 現在の状態（2026-09-10）

直近のマージ:

| PR | 内容 |
| --- | --- |
| #50 | 管理UI「ストック全承認→予約」ボタン |
| #49 | 生成だけ上位モデル、拒否と effort の扱い |
| #48 | X API 枠切れの耐性、評価指標を計測タイミングに揃える |
| #47 | 採点の正しさ（ノイズ学習の停止、予測力の検証） |
| #46 | チャンネル投稿をメモ扱い、画像 403 の案内 |
| #45 | Q1 に答えると別の日の Q10 が消費される問題 |

未確認のもの。それぞれ「どうやって確かめるか」を添える:

| 項目 | 確かめ方 |
| --- | --- |
| 本番データでの採点の再現性と予測力 | `diagnoseScoring`（`Metrics.gs`）を実行し、出力を貼ってもらう。順位相関が「偶然でも出る大きさ」を超えていれば当たっている |
| 手動投稿 vs 生成投稿の交絡 | `reportAxesByOrigin`（`Metrics.gs`）。符号が反転する軸があれば、その軸は群の違いを測っている |
| 画像つき投稿が X に添付されるか | 画像つきで回答 → 下書き → 投稿。`Log` の `x_media_upload` を見る |
| Fable 5.1 での生成物の質 | 切り替え後の下書きを数日ぶん見る。**質が落ちていたら `buildStylePrompt`（`Voice.js`）の指示を削る**方向で調整する（指示が細かすぎると質が落ちる性質がある） |
| X API の枠が 9/1 に復帰したか | `weeklyMetricsReport` 後の Slack「計測データの状態」で最終計測日が更新されているか |

既知の制約:

- 標本が少ない（軸スコアと実測が揃った投稿が数十〜百件台）。有意な軸は少ない。件数が増えれば判定の厳しさは自動で緩む
- 生成の不合格理由の多くは「具体的な事実が足りない」。**モデルを強くしても、インタビューで語られていない事実は書けない。** `missingInfoHints` が次の回答で何を聞くべきかを返す

## 9. ユーザー（中井さん）について

- 数字と根拠で話すと納得する。「採点が正しくないとアドバイスが正しいと感じられない」と言われて、実データで検証したら実際に正しくなかった
- 仮説を出してくれることが多く、当たっている（「前日の質問に答えているのが原因では」等）。まず仮説を検証する
- 自分の操作が原因のエラーでも「エラーがあるなら直さないとね」と言う。原因を人のせいにせず仕組みで直す
- 実装したら「マージして」の一言で進む。いまは確認なしでマージしてよい
- 説明は日本語。関数を足したら `.gs` のファイル名を必ず添える
- モバイルで操作していることが多い。Slack と管理UI がインターフェース。GAS エディタの関数も手で実行する
- 依頼は短い。一文に複数の意図が入っていることがある（例: 「API制限でメトリクスを取りそびれた。評価指標を更新してほしい」= 枠切れの対処 と 評価指標の見直し の両方）

## 10. 触ってはいけないもの・慎重に扱うもの

- 管理UIのドメイン制限
- `Stock` シートの既存列・既存行（削除は相談してから）
- `SCORING_ANCHOR_IDS`（採点の物差し。変えると過去のスコアと比較できなくなる。`resetScoringAnchors` は意図してのみ）
- `CLAUDE_MODEL_SCORE`（同上）
- `AXIS_FLOORS` の本人らしさの下限（制約条件。データが負でも外さない）
- `Voice` シート（文体サンプル。ここが壊れると全部が壊れる）

---

## 変更履歴（引き継ぎ用）

**引き継いだ側は、作業のたびにここへ追記する。** 次の担当が最初に読む場所。書くこと: 日付、PR 番号、何を変えたか、何を実データで確かめたか、**確かめていないこと**、ユーザーから言われたこと。

### 2026-09-10 — Claude（この資料の作成まで）

- PR #45〜#50 をマージ済み。内容は §8 の表
- 確かめたこと: 採点の重み学習が未学習データで +0.04 だったこと（66件）、X API の枠切れ時に API 呼び出しが1回で止まること、Fable 5.1 への切り替えで送るリクエストの形（Node で差し替えて確認）
- 確かめていないこと: §8 の「未確認」の表すべて。本番の `diagnoseScoring` は一度も実行結果を見ていない
- ユーザーの直近の意向: ツイート本文の質を上げたい。生成を Fable 5.1 に切り替え、必要なら GPT-6 Astra も試す。**しばらく別のアシスタントに任せた後、Claude に戻す予定**。戻ってきたときにこの履歴から再開できるようにしておくこと
